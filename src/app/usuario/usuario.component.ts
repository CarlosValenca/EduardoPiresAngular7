import { Component } from '@angular/core';

@Component({
    // É importante colocar OU uma página html que vai ter as rotas
    // OU o html quando o mesmo for simples como o que está abaixo
    template: '<router-outlet></router-outlet>'
})
export class UsuarioComponent {}